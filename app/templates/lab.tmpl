{{template "header" .}}

{{ define "card" }}
    <div class="card">
        <div class="title">
            <h2>{{ .Title }}</h2>
        </div>
        <div class="description">
            <p>{{ .Image }}</p>
        </div>

        {{if false}}
        <div class="button up">
            <span>Stop</span>
        </div>
        {{else}}
        <form action="./lab" method="POST">
            <input type="hidden" name="lab" value="{{ .UUID }}">
            {{ if false }}
                <input class="button up" type="submit" name="action" value="Stop">
            {{ else }}
                <input class="button" type="submit" name="action" value="Start">
            {{ end }}
        </form>
        {{ end }}
    </div>
    {{ end }}

<div class="cards">

    {{ range .Labs}}
    {{ template "card" .}}
    {{ end }}

</div>


{{template "footer" .}}